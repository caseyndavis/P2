//project 2 js functions

var isArmstong = function(n){
	"use strict";
	//convert to string
	n = String(n);
	for (var i = 0; i< n.length; ++i){
		var sum = sum + Math.pow(n[i], n.length)
		return sum
	}
}